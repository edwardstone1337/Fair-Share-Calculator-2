export const metadata = {
  robots: { index: false, follow: false },
  title: "Dashboard Preview — Fair Share Calculator",
};

export default function DashboardPreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
