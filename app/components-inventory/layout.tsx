export const metadata = {
  robots: { index: false, follow: false },
  title: "Components Inventory — Fair Share Calculator",
};

export default function ComponentsInventoryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
